<!--index.wxml-->
<view class="container">
  <camera device-position="back" flash="off" binderror="error" class="camera"></camera>
  <button type="primary" bindtap="takePhoto">拍照</button>
  <view>预览</view>
  <image mode="widthFix" src="{{src}}" class="preivew"></image>
</view>

<!-- ===== 暗号：数乘 ====== -->
